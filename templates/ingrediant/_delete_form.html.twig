<form method="post" action="{{ path('app_ingrediant_delete', {'id': ingrediant.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ ingrediant.id) }}">
    <button class="btn">Delete</button>
</form>
