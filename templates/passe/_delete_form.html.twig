<form method="post" action="{{ path('app_passe_delete', {'id': passe.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ passe.id) }}">
    <button class="btn">Delete</button>
</form>
