<form method="post" action="{{ path('app_commantaire_delete', {'id': commantaire.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ commantaire.id) }}">
    <button class="btn">Delete</button>
</form>
